import 'package:flutter/material.dart';

class AppConstants {
  // Font Sizes
  static const double titleFontSize = 30.0;
  static const double headerFontSize = 20.0;
  static const double largeFontSize = 16.0;
  static const double mediumFontSize = 14.0;
  static const double smallFontSize = 12.0;

  // Colors
  static const Color primaryColor = Colors.purpleAccent;
  static const Color backgroundColor = Colors.black;
  static const Color surfaceColor = Color(0xFF212121);
  static const Color cardColor = Color(0xFF424242);
  static const Color borderColor = Color(0xFF616161);
  static const Color dividerColor = Color(0xFF757575);
  static const Color textPrimaryColor = Colors.white;
  static const Color textSecondaryColor = Color(0xFFB3B3B3);
  static const Color textHintColor = Color(0xFF9E9E9E);
  static const Color disabledColor = Colors.grey;

  // Status Colors
  static const Color successColor = Colors.green;
  static const Color errorColor = Colors.red;
  static const Color warningColor = Colors.orange;
  static const Color infoColor = Color(0xFF64B5F6);
}
